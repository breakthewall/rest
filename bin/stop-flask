#!/bin/bash

redis=""

docker network disconnect galaxy "$(REDIS=$redis docker inspect -f '{{.Name}}' $(docker-compose -f docker-compose-flask.yml ps -q)  | cut -c2-)"
REDIS=$redis docker-compose -f docker-compose-flask.yml down
