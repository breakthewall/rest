#!/bin/bash

redis=""

docker network disconnect galaxy "$(docker inspect -f '{{.Name}}' $(REDIS=$redis docker-compose -f docker-compose-flask.yml ps -q)  | cut -c2-)"
REDIS=$redis docker-compose -f docker-compose-flask.yml down
